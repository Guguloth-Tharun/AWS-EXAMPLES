{
  "name": "AWS Solutions Architect",

  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",

  "remoteEnv": {
    "AWS_CLI_AUTO_PROMPT": "on-partial"
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y unzip jq zip && curl \"https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip\" -o awscliv2.zip && unzip -o awscliv2.zip && sudo ./aws/install && rm -rf aws awscliv2.zip",

  "customizations": {
    "vscode": {
      "extensions": [
        "amazonwebservices.aws-toolkit-vscode",
        "ms-python.python",
        "hashicorp.terraform",
        "GitHub.copilot"
      ]
    }
  }
}
